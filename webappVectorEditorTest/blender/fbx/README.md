# FBX assets (local)

Put `.fbx` files here for Blender workflows—manual import, linking, or future automation from `import_vector_json.py`.

Large files: consider [Git LFS](https://git-lfs.com/) (add `*.fbx` to `.gitattributes` if needed).
